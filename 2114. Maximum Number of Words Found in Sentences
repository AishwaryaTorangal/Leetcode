class Solution:
    def mostWordsFound(self, s: List[str]) -> int:
        max_words = 0
        
        for string in s:
            word_count = string.count(" ") + 1  
            max_words = max(max_words, word_count)
            
        return max_words
        
